import React from "react";

type Props = {
  reportNotificationMethods: (notificationMethods: string[]) => void;
};

function NotificationMethods({}: Props) {
  return <div>NotificationMethods</div>;
}

export default NotificationMethods;
